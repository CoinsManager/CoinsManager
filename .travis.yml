language: node_js
node_js:
  - "0.10"
before_install:
  #- "curl -L http://git.io/3l-rRA | /bin/sh"
  - curl https://install.meteor.com | /bin/sh
  - npm install -g meteorite
  - npm install -g laika
  - npm install -g coffee-script
services:
    - mongodb
env:
    - LAIKA_OPTIONS="-t 5000 --ui bdd --compilers coffee:coffee-script --reporter nyan -V"
notifications:
  email:
    - coinsmanager@gmail.com
