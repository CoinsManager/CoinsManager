template(name="userAddresses")
    h1.addresses_title Your addresses

    div.addresses
        {{#isolate}}
        {{#each sortAddresses userAddresses}}
            {{> addressItem}}
        {{/each}}
        {{/isolate}}

        {{#if showCoinForm}}
            {{> addAddress}}
        {{else}}
            {{> emptyForm}}
        {{/if}}

        {{#if showImportForm}}
            {{> fetchAddresses}}
        {{else}}
            {{> emptyImportForm}}
        {{/if}}
