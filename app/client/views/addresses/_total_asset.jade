template(name="totalAsset")
    {{totalValue}}
    select.fiat-list
        {{#select userFiat}}
        {{#each fiatList}}
        option(value="{{this}}") {{this}}
        {{/each}}
        {{/select}}
