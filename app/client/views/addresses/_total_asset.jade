template(name="totalAsset")
    {{totalValue}}
    select(name="fiat-list")
        {{#select userFiat}}
        {{#each fiatList}}
        option(value="{{this}}") {{this}}
        {{/each}}
        {{/select}}
